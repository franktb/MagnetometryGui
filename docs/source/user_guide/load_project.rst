Loading a project
=================

To load a project, select {\em Load Project} within the drop-down menu {\em File} (see Fig.\ref{fig:drowdown_import}).
This will open a file dialogue.
Within the file dialogue navigate to the folder where the desired project was stored and select the corresponding \verb|*.json| file.